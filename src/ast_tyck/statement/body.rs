use super::*;

impl Check for Body {
    fn check(
        &self,
        classes: &Classes,
        functions: &Functions,
        current: &FunctionContext,
    ) -> Result<Type, String> {
        unimplemented!()
    }
}
