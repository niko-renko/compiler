use super::*;

impl Check for Print {
    fn check(
        &self,
        classes: &Classes,
        functions: &Functions,
        current: &FunctionContext,
    ) -> Result<Type, String> {
        unimplemented!()
    }
}
