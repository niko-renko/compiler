use super::*;

impl Check for Return {
    fn check(
        &self,
        classes: &Classes,
        functions: &Functions,
        current: &FunctionContext,
    ) -> Result<Type, String> {
        unimplemented!()
    }
}
